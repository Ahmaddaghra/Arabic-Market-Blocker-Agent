import React,{useState} from 'react';import{createRoot}from'react-dom/client';import'./styles.css';
type Result={status:string;verdict:string;url:string;planner:string;supportMessage?:string;findings:Array<{id:string;title:string;severity:string;summary:string;impact:string;actual:string;expected:string;locator:{strategy:string;value:string};evidenceScreenshot:string;generatedTest:string}>;screenshots:{baseline:string;arabic:string};comparison:{english:string;arabic:string}};
const stages=['Safe URL check','Browser launch','DOM analysis','English baseline','Arabic persona test'];
function App(){const[url,setUrl]=useState('');const[loading,setLoading]=useState(false);const[result,setResult]=useState<Result|null>(null);const[error,setError]=useState('');async function run(){setLoading(true);setError('');setResult(null);try{const response=await fetch('/api/audits',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({url})});const data=await response.json();if(!response.ok)throw new Error(data.error||'Audit failed safely.');setResult(data)}catch(e){setError(e instanceof Error?e.message:'Audit failed safely.')}finally{setLoading(false)}}return <><header><div className="brand"><span className="mark">ع</span>Arabic Market Blocker Agent</div><a href="#scope">Scope</a></header><main><section className="runner"><label htmlFor="url">Public signup form URL</label><div className="run-row"><input id="url" value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://example.com/signup"/><button onClick={run} disabled={loading||!url}>{loading?'Running bounded audit…':'Run Saudi signup audit'}</button></div><p className="limits">◈ Public signup forms only · No CAPTCHA · No auth wall</p></section><section className="timeline">{stages.map((s,i)=><div className={loading?'active':result?'done':''} key={s}><span>{result?'✓':i+1}</span><b>{s}</b><small>{result?'Completed':loading&&i===0?'In progress':'Bounded step'}</small></div>)}</section>{error&&<section className="unsupported"><strong>Unsupported or blocked target</strong><p>{error}</p><span>The audit stopped without submitting data.</span></section>}{result&&result.status==='unsupported'&&<section className="unsupported"><strong>Unsupported site</strong><p>{result.supportMessage}</p><span>This is an intentional bounded fallback, not a failed run.</span></section>}{result&&result.status==='completed'&&<Results result={result}/>}<section id="scope" className="scope"><h2>Intentionally bounded</h2><p>One public standard signup flow, one Saudi market rule pack, no CAPTCHA bypass, no authentication wall, no payment, and no claim of certification.</p></section></main><footer>Safe URL resolution · Private IP blocking · Hard timeout · Rate limited</footer></>}
function Results({result}:{result:Result}){const first=result.findings[0];return <section className="results"><aside><div className={result.findings.length?'verdict bad':'verdict good'}><small>Tested flow verdict</small><h1>{result.verdict}</h1><p>{result.findings.length?'Evidence-backed issues can prevent Saudi users from completing this tested flow.':'No blockers were observed within the bounded checks that ran.'}</p></div>{result.findings.map((f,i)=><article key={f.id}><span>{i+1}</span><div><b>{f.title}</b><p>{f.summary}</p></div><em>{f.severity}</em></article>)}<div className="planner">Planner: <b>{result.planner}</b></div></aside><div className="detail"><div className="comparison"><figure><figcaption>English baseline</figcaption><img src={result.screenshots.baseline}/><p>{result.comparison.english}</p></figure><figure><figcaption>Arabic persona</figcaption><img src={result.screenshots.arabic}/><p>{result.comparison.arabic}</p></figure></div>{first?<><div className="evidence"><div><small>DOM locator</small><code>{first.locator.strategy}: {first.locator.value}</code></div><div><small>Business impact</small><p>{first.impact}</p></div><div><small>Expected</small><p>{first.expected}</p></div><div><small>Actual</small><p>{first.actual}</p></div></div><div className="code-head"><b>Generated regression test</b><button onClick={()=>navigator.clipboard.writeText(first.generatedTest)}>Copy</button></div><pre><code>{first.generatedTest}</code></pre></>:<div className="empty"><h2>No blockers found in tested flows</h2><p>This means only that the bounded checks completed without evidence of a blocker. It is not a readiness certificate.</p></div>}</div></section>}
createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);
